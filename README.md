# Redis Admin

A Simple GUI client for Redis.

## Client

powered by `react` and `antd`, run: 

```bash
$ cd client
$ yarn start
```

## Server

powered by `nodejs` and `expressjs`, run:

```bash
$ cd server
$ yarn start
````